Software Requrements
- vaguely unixlike workstation
- ruby 2.x with Bundler ( `gem install bundler` if you're not sure )
- packer (install from your package manager or https://www.packer.io/downloads.html )
- apachebench ( `brew install homebrew/apache/ab` if you're on OSX using Homebrew)
Other Reqs
- Have an AWS account with your local environment variables of AWS_ACCESS_KEY_ID and AWS_SECRET_ACCESS_KEY filled out appropriately. You'll get yelled at if they aren't filled out.



